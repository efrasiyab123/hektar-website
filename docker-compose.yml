# c_nginx (NPM) ile aynı harici ağ — Proxy Host: http://c_hektar:3007, şema http
# Sunucuda: mevcut proxy_net kullanılır (docker network ls).
services:
  c_hektar:
    build: .
    image: hektar-website:latest
    container_name: c_hektar
    restart: unless-stopped
    ports:
      - "3007:3007"
    networks:
      - proxy_net

networks:
  proxy_net:
    external: true
    name: proxy_net
